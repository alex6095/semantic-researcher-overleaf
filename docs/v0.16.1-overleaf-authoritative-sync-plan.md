# v0.16.1 Overleaf-authoritative Local Replica plan

## Purpose

Overleaf is the canonical project state. A selected local folder is a durable,
editable working copy used by VS Code, Zed, shells, compilers, and agents. A
local change is not canonical until Overleaf accepts it. The synchronizer must
retain local intent while it obtains current remote state, submits an operation,
and observes the accepted remote result.

This is the durable task checklist. Update phase status and the work log in the
same commit as every material implementation or design decision.

## Recovery and branch policy

- [x] Preserve released source as annotated tag `v0.16.0-recovery` at
      `93d7db5d63b8ace495162e74d3e5f3f21c834f17`.
- [x] Start implementation on `release/0.16.1`; never move `v0.16.0`.
- [x] Bump main and Remote Pack manifests and lockfiles to `0.16.1`.
- [ ] Push the branch after the first implementation checkpoint passes focused
      tests.
- [ ] Never replace a published release asset with a different same-version
      build.

## Non-negotiable invariants

1. An acknowledged Overleaf document or project-tree revision is the only
   canonical state.
2. The local tree remains normally editable. An agent or shell never needs to
   coordinate with VS Code before saving.
3. A dirty local editor buffer is never overwritten by remote input. A clean
   editor model may refresh from synchronized local disk state.
4. Event timing, mtime, and log order are diagnostic hints, never proof for
   conflict ordering or deletion.
5. Uncertain states preserve both copies and enter a durable conflict; they
   never select a winner silently.
6. `.output/**` and extension metadata are internal. A project PDF, PNG, ZIP,
   CSV, and other files remain syncable by default.

## Operation model

| Kind | Canonical server operation | Required precondition | Conflict policy |
| --- | --- | --- | --- |
| UTF-8 document update | join current doc, rebase, apply OT, observe ack | document id and current version | 3-way merge; overlap conflicts |
| File/media update | guarded upload/replace file entity | entity id and remote digest/revision | preserve both blobs |
| File creation | upload then add file entity | expected parent/tree state | same-name remote entity conflicts |
| Folder creation | add folder entity | expected parent/tree state | incompatible existing entity conflicts |
| Rename/move | entity-id tree operation | entity id, parent, tree state | remote move/rename conflicts |
| Delete | entity-id delete/tombstone operation | entity id/revision and clean local baseline | local divergence retains file |

## Durable state model

The manifest records last accepted remote state. Add a durable
pending-operation journal instead of treating a watcher event as an immediate
imperative request.

Every journal entry needs:

- operation id, kind, affected paths, and entity id when known;
- accepted remote identity/revision/digest and parent identity;
- stable local snapshot digest and recoverable snapshot/staging reference;
- creation time only for diagnostics, never ordering;
- lifecycle: `queued`, `reconciling`, `submitted`, `accepted`, or
  `conflict`.

The first manifest-v3 slice covers regular file updates and deletes. Its
recoverable source is the selected local working-copy path; the journal binds
that source to a stable digest before remote I/O. It does not duplicate file
bytes into a staging area. Manifest v4 additionally records the verified
Overleaf document/file entity ID and stable local `dev:ino` identity for each
regular file. A missing or legacy identity cannot prove a move; tree
operations will add the required entity and parent identities before they can
share this journal.

Coalescing must retain the same user-visible outcome. A known rename must not
degrade into an unrelated delete plus create.

## Phase checklist

### Phase 0 - Baseline and schema

- [x] Record v0.16.0 recovery anchor and create v0.16.1 branch.
- [x] Add this plan and mark source manifests as 0.16.1.
- [x] Inventory manifest, retry, watcher, and remote-file paths against the
      operation model.
- [x] Define manifest-v3 journal schema migration and backward compatibility.
- [x] Add manifest-v4 remote entity and stable local identity fields, retaining
      legacy records only as non-move evidence.
- [x] Add focused tests for journal recovery before production-path changes.

### Phase 1 - Local intent journal and status

- [x] Persist regular-file update/delete intent before remote I/O begins; clear
      it only after an acknowledged remote result or a verified equal state.
- [x] Coalesce repeated stable writes on the same path to the final local
      working-copy digest without relying on event timing.
- [x] Expose `Synced`, `Pending upload`, `Offline queued`,
      `Reconciling`, and `Needs attention` with counts.
- [x] Keep offline work non-blocking; known connection-unavailable failures are
      queued and logged rather than surfaced as a sync-failure notification.
      Never show a modal merely because the
      connection is unavailable.
- [x] On reload/Remote SSH reconnect, re-read the authoritative remote path and
      replay the regular-file intent through the existing guarded merge/delete
      path; a concurrent change remains a durable conflict.
- [x] Extend the journal to file creation, folders, rename/move, and recursive
      delete only after their entity/tree preconditions are recorded.

### Phase 2 - Text documents

- [x] Use the official versioned `joinDoc` catch-up protocol when a subscribed
      cache needs refresh; validate every returned text operation or retry a full snapshot.

- [x] Convert official `otUpdateError` events into explicit failed writes;
      force full-snapshot verification and retain unresolved local intent.
- [x] Replay a source-identified in-flight OT once with official
      `dupIfSource` duplicate suppression; sender acknowledgement, explicit
      rejection, or missing identity stays fail-closed.
- [x] Replace direct VFS fuzzy patch rebasing with an explicit three-way merge;
      unmergeable overlap remains a conflict.
- [x] Before each submission, obtain a coherent subscribed `joinDoc`
      revision and rebase local text against it (the protocol fallback, which
      has no joinDoc subscription, retains its explicit response path).
- [x] Submit an OT update with current base version and wait for matching
      applied operation/revision before accepting it in the manifest.
- [x] Reconcile collaborator socket operations into clean local documents.
      A small bounded version gap is queued in exact revision order; a large
      gap immediately invalidates the cache and preserves the merge base.
- [x] Make malformed/gapped collaborator updates, bad acks, and unjoinable
      docs fail closed into snapshot retry/conflict rather than stale pushes.

### Phase 3 - Files and project tree

- [x] Record verified remote document/file and stable local `dev:ino` identity
      for regular files; incomplete or migrated identity never proves a move.
- [x] Journal a guarded regular-file move before its remote mutation; replay it
      only after revalidating source entity identity, destination absence, and
      the unchanged local inode/digest.
- [x] Complete cross-parent basename changes as the same entity's
      rename-then-move sequence; a proved halfway path resumes the second step
      rather than fabricating the requested destination in the local cache.
- [x] Correlate watcher-observed local renames only through an exact inode match;
      a short delay may collect a delete/create pair but is never ordering proof.
- [x] Verify the destination retains the original Overleaf entity after a move;
      any mismatch or occupied destination preserves both sides as a conflict.
- [x] Model create, update, delete, rename, move, and mkdir as guarded entity
      operations.
- [x] For mkdir, persist the missing-path and parent-folder precondition before
      the request; persist the returned folder ID before final verification; a
      same-name folder after an unacknowledged request is a conflict.
- [x] Upload source PNG/PDF/ZIP/CSV by default; exclude only configured
      generated output paths, `.output`, and known TeX intermediates.
- [x] Verify remote entity identity and postconditions after every mutation.
- [x] Preserve a binary conflict as two recoverable copies; never byte-merge.
- [x] Block folder delete containing unresolved descendant operations.
- [x] Block folder rename/move containing unresolved descendant operations.

#### Active guarded regular-file-create slice

- [x] Add a distinct durable `create` journal record with the verified parent
      folder identity, exact expected-missing state, and server-issued file ID.
- [x] Persist the returned document/file identity before any postcondition
      lookup; never acknowledge a same-name/same-byte unproven entity.
- [x] Replay only the exact journaled entity at the journaled parent; a
      disappeared or substituted entity is a durable conflict.
- [x] Cover response-loss, restart, parent replacement, and local advancement
      boundaries with deterministic tests.

#### Active guarded existing-file mutation identity slice

- [x] Bind regular-file update/delete intent to the exact accepted remote
      entity ID and parent ID, not only its digest.
- [x] Require exact entity/parent proof at every delete or binary-replacement
      stage, response-loss recovery, and final postcondition.
- [x] Treat an identity-less legacy pending operation or same-byte entity
      replacement as a durable conflict rather than silently adopting it.
- [x] Cover live/restart same-byte replacement, ambiguous staging, and positive
      exact-ID delete/replacement paths with deterministic tests.

#### Active guarded move-parent identity slice

- [x] Bind every regular-file move journal to its exact source and destination
      parent-folder IDs, in addition to the moved entity ID.
- [x] Revalidate both recorded parents immediately before the rename/move request,
      pass the recorded source-parent ID into the VFS guard, and verify the
      destination entity and parent after it.
- [x] Make an identity-less legacy file-move record a durable conflict unless
      both historical parents can be proven from the trusted manifest.
- [x] Cover source/destination same-path parent replacement boundaries for both
      regular-file and directory moves; no remote mutation may occur.

#### Active local watcher move-source claim

- [x] Claim an exact inode/digest move source before any awaited remote proof so
      a slow binary/media move cannot let its held source delete start on a
      sibling watcher queue.
- [x] Regress a delayed PNG/media move past the candidate window and require the
      original remote entity to arrive at the destination with no stale delete
      journal or conflict.

#### Active watcher and socket tree-ordering slice

- [x] Claim a folder move from a child-first local watcher event before that
      child can be misclassified as an unrelated file create/move.
- [x] Defer a recursive local child update until each new parent folder has a
      durable remote identity; an agent or shell can write the tree in any
      watcher order.
- [x] Make delayed self socket rename/move echoes idempotent after the VFS has
      already applied the same entity ID and parent transition.
- [x] Fence source, destination, and transient rename-first paths for an active
      local folder-move journal; replay remote events only after accepted
      manifest rekeying.
- [x] Recheck that fence after VFS-event debounce and immediately before its
      queued pull runs. Tag each retained event with the exact journal ID, and
      reclassify it when that journal becomes a durable conflict rather than
      leaking it into a later move with the same transient basename.
- [x] When a local writer advances a file while its guarded first create is
      accepted, convert the exact created entity into a durable update/delete
      intent and send the latest local state; do not emit a false conflict.

#### Active tree-operation slices

- [x] Guarded durable mkdir with remote parent and created-folder identity.
- [x] Bind recursive folder delete staging to folder/parent identity and prove
      its postcondition before clearing descendant state.
- [ ] P0 release boundary: hosted Overleaf's public tree endpoints have no
      conditional parent/revision precondition. Obtain a server-side CAS API or
      explicitly approve upstream entity-ID last-operation semantics before
      claiming strict local-path conflict guarantees or shipping this slice.
- [x] Model folder rename/move as an ID-preserving durable operation; block it
      when a descendant operation is unresolved.
- [x] Recheck conflict and conflict-resolution intersections immediately before
      an accepted folder move rekeys local state; retain the durable move journal
      rather than stranding a late conflict at its old path.
- [x] Expose file/binary conflict choices (Keep Local, Keep Overleaf, Keep
      Both) without discarding journal evidence.
- [x] Expose folder/tree Keep Overleaf and Keep Both only through a separate
      guarded recursive-resolution transaction.

### Phase 4 - Inbound reconciliation and conflict UX

- [x] Do not use an HTTP text snapshot as an accepted outgoing or compile baseline until serialized joinDoc verification performs guarded reconciliation; buffer and replay startup events first.
- [x] Apply remote changes directly only to clean local paths; a dirty open
      document (or dirty descendant of a deleted tree) becomes a durable conflict
      before its backing file is changed.
- [x] Add Base/Local/Overleaf 3-way text diff with Accept Overleaf, Accept
      Local, and Apply Merged Result.
  - [x] Persist a bounded text-merge proof at conflict creation: the common
        base bytes/digest and the exact remote file and parent identities.
  - [x] Provide an extension-owned, CSP-locked three-pane Base/Local/Overleaf
        preview and an in-memory editable result. Closing or reloading it must
        leave both canonical copies and the manifest unchanged.
  - [x] Apply only an explicitly confirmed result through a separate durable
        text-merge transaction: recheck the local inode/digest, pending and
        folder-move fences, remote bytes, entity and parent immediately before
        each mutation, then call the guarded Local Replica push path.
  - [x] Recover every persisted merge phase after restart; a changed local or
        remote proof must remain a conflict rather than auto-pushing a stale
        result.
  - [x] Add deterministic coverage for preview/cancel, clean and overlapping
        merges, dirty editors, local inode changes, same-byte remote entity
        replacement, pending move fences, and interrupted phase recovery.
- [x] Add Keep Local, Keep Overleaf, and Keep Both for file/binary conflicts.
- [x] Add Keep Overleaf and Keep Both for folder/tree conflicts through a
      separate guarded recursive-resolution transaction.
- [x] Preserve a Keep Both artifact until the verified remote decision and
      canonical local finalization complete.

- [x] Refuse Keep Local and every automatic local write while its exact editor
      document is dirty, preventing a saved disk snapshot or merge writeback from
      superseding unsaved text.

#### Active conflict-resolution slice

- [x] Add manifest-v10 resolution transactions and bounded resolution history without
      repurposing the pending-operation keyspace.
- [x] Offer file/binary choices in the Local Replica settings flow: Keep Local,
      Keep Overleaf, and Keep Both (only when a local file exists to preserve).
- [x] For Keep Overleaf and Keep Both, verify the recorded Overleaf revision;
      for Keep Both preserve a local artifact under ignored extension metadata,
      then apply the remote canonical file atomically; never mutate Overleaf.
- [x] Recover a file/binary resolution transaction after restart without clearing
      the blocking conflict or losing its local artifact, including the
      write/delete-before-phase-persist boundary.
- [x] Cover remote/local advancement races, pending-folder-move isolation, and
      UI selection/cancellation.
- [x] Keep folder Keep Overleaf/Keep Both as a separate guarded tree transaction;
      expose only the no-remote-mutation, remote-canonical choices after recursive
      digest/inode staging; folder Keep Local remains unavailable without server-side CAS.

#### Next guarded folder-resolution transaction

- [x] Add manifest-v11 folder-resolution records separate from file resolution
      and pending-operation journals, with root recursive revision, root/parent
      entity IDs, local directory identity, artifact/staging paths, and durable
      phase transitions.
- [x] Implement the remote-folder-deletion branch internally: preserve the
      complete local tree by durable atomic rename to an ignored artifact
      (Keep Both) or private Keep Overleaf guard (Keep Overleaf), then resume the
      `prepared ? local-preserved ? canonical-applied` path after restart.
      This API is intentionally not shown in the UI yet.
- [x] Re-verify root tree digest plus root/parent entity IDs immediately before
      any local tree swap and after remote-tree staging; never use a path name or
      timestamp as proof.
- [x] Revalidate a deferred remote replacement stage before moving the canonical
      local tree. Rebuild only a missing protected stage from the still-proven
      remote tree; preserve a partial or mismatched stage as blocking evidence.
- [x] Block on a dirty open descendant, ignored local or remote descendant,
      sync conflict/active resolution, or pending operation/move intersecting
      the source or destination tree.
- [x] For folder Keep Both, preserve the complete local tree under ignored
      extension metadata before installing remote canonical state; do not upload
      or mutate Overleaf.
- [x] Recover a remote-folder replacement when the local tree was atomically
      preserved but the `local-preserved` phase did not persist; recognize only
      the exact protected tree and missing canonical-root combination.
- [x] Defer a transient persisted folder-resolution proof without failing the
      initial pull, then single-flight replay a non-blocked decision on reconnect.
- [x] Resume a persisted non-blocked folder decision before ordinary startup
      overwrite, so an interrupted remote-stage phase write cannot be mistaken
      for an unrelated divergent local folder.
- [x] If a protected remote stage is changed after local preservation, retain
      the complete artifact/guard and the stage as durable evidence, then block
      on restart rather than attempting an automatic restore or overwrite.

- [x] Detect a partial protected remote stage left before `remote-staged`
      persists; on restart retain it as evidence and block without moving the
      canonical local tree or overwriting Overleaf.

- [x] Make remote folder deletion and remote folder replacement resume safely
      across every persisted phase. A crash must retain at least one recoverable
      copy of each tree and never finish by guessing a same-name replacement.
- [x] Cover remote/local tree advancement, same-digest entity replacement,
      recursive artifact integrity, stage/install crash boundaries, watcher/scan
      echoes, and source/destination folder-move intersections before enabling UI.

#### Active folder conflict UI exposure (remote-authoritative only)

- [x] Expose only folder conflicts whose local and Overleaf states are each a
      directory or missing; do not present a choice for type-swapped trees.
- [x] Never offer folder Keep Local. Hosted Overleaf exposes no conditional
      tree-mutation API, so the UI must only offer the no-remote-mutation
      Keep Overleaf and (for an existing local tree) Keep Both paths.
- [x] Route both choices through the durable folder transaction, preserving its
      tree/inode/identity/pending/dirty/watcher fences and restart replay.
- [x] Cover picker choices, cancellation, Keep Both artifact routing, Keep
      Overleaf guard routing, and the existing folder transaction race suite
      through the registered settings command.

### Phase 5 - Acceptance matrix and release

- [ ] Closed and open text edits from VS Code, Zed, shell, and agent.
- [ ] PNG/PDF/ZIP create, replace, delete, rename, and folder move.
- [ ] Web edits while local is clean, dirty, offline, and reconnecting.
- [ ] Long local writer, atomic-save editor, and local compiler output.
- [ ] Remote delete versus prompt local recreation; subtree conflicts.
- [ ] Reload/restart journal recovery and account/project isolation.
- [ ] Full test suite, lint, production audits, package integrity, and signed-in
      Remote SSH / web validation.

## Work log

| Date | Status | Evidence / decision |
| --- | --- | --- |
| 2026-08-10 | Started | v0.16.0 anchored at 93d7db5; work begins on release/0.16.1. |

| 2026-08-10 | Implemented | Manifest v3 retains pending regular-file updates/deletes before remote I/O, removes them only after acknowledgement, and reconciles them after restart. Focused tests cover interrupted update/delete, collaborator conflict preservation, and v2 migration; `npm run lint` and the full extension suite pass (434 passing). |

| 2026-08-10 | Implemented | Live reconnect now drains accepted sync work then replays the durable regular-file journal once behind a sync fence. Pending intent is acknowledged on a pull only when local and Overleaf exactly match that intent. Local Replica status exposes pending, offline, reconciling, and attention counts; known offline writes remain queued without a warning notification. Focused tests cover update/delete replay, duplicate connection signals, remote conflict preservation, and lost acknowledgement recovery. |
| 2026-08-10 | Verified | `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 438 extension-host tests. |
| 2026-08-10 | Implemented | Cross-checked current official Overleaf `DocumentContainer.joinDoc` and `ShareJsDoc.catchUp`: cached subscribed text now requests `joinDoc(docId, version, options)`, validates every returned OT operation in sequence, and falls back to a full snapshot on missing, malformed, or non-text catch-up. Focused protocol/catch-up/fallback tests pass (3 passing), plus compile and lint. |
| 2026-08-10 | Corrected | An uncertain text-write acknowledgement now forces a full authoritative snapshot instead of trusting a same-version cache; ordinary subscribed refreshes retain validated versioned catch-up. Regression tests cover collaborator-race and delayed-ack rejection cases. |
| 2026-08-10 | Verified | `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 441 extension-host tests. |
| 2026-08-10 | Implemented | Registered official `otUpdateError` wire events and made a pending text save fail closed: invalidate its cache, force a full authoritative snapshot, and keep the local intent as a conflict if the update was not applied. Focused listener and in-flight rejection tests pass; lint is clean. |
| 2026-08-10 | Verified | `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 443 extension-host tests. |
| 2026-08-10 | Implemented | Matched official ShareJS reconnect handling for a source-identified in-flight text update: retry once with `dupIfSource`, never repeat after an observed sender acknowledgement or explicit error, and verify a duplicate rejection with a full authoritative snapshot. Focused retry/readback tests pass; compile and lint are clean. |
| 2026-08-10 | Verified | `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 445 extension-host tests. |
| 2026-08-10 | Implemented | Removed direct-VFS fuzzy patch rebasing. Clean non-overlapping edits use an explicit 3-way merge; any overlapping text raises a merge conflict. Focused test plus compile and lint pass. |
| 2026-08-10 | Verified | `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 446 extension-host tests. |
| 2026-08-10 | Implemented | Matched official ShareJS out-of-order handling: bounded per-document operation queues apply only exact next revisions; stale duplicates are ignored, and a missing predecessor times out into an authoritative snapshot instead of a fuzzy patch or silent winner. Focused OT regression suite passes (13 passing), with compile and lint clean. |
| 2026-08-10 | Verified | `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 447 extension-host tests. |
| 2026-08-10 | Implemented | Manifest v4 records a verified Overleaf document/file entity ID and stable local `dev:ino` identity per regular file. Legacy or invalid records are retained only as non-move evidence; focused manifest migration/identity/invalid-schema checks pass (8 passing), with compile and lint clean. |
| 2026-08-10 | Verified | `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 448 extension-host tests. |
| 2026-08-10 | Implemented | Manifest v5 adds a durable guarded regular-file move journal. A watcher deletion is held only briefly; a move is accepted only when destination `dev:ino`, digest, current source entity ID/type, and remote destination absence all match. The VFS rechecks the expected entity ID immediately before its tree API call. Restart replay, unavailable-initial-proof reconnect, occupied-destination conflict preservation, ordinary delete release, and entity continuity are covered by focused extension-host tests, with compile and lint clean. |
| 2026-08-10 | Corrected | Move correlation now applies only to regular files and yields when an ancestor folder reconciliation owns the destination queue. Directory events and folder reconciliation therefore retain their guarded existing path rather than competing with a child entity move. |
| 2026-08-10 | Verified | `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 453 extension-host tests. |

| 2026-08-10 | Implemented | Compile-output filtering is now path-based: `.output` and the configured compile-output root are internal, while source PNG/PDF/ZIP/CSV files—including ordinary `main.pdf` and `output.pdf` names—sync by default. Existing explicit ignore patterns still apply. Focused pull/push and output-path regression tests pass with compile and lint. |
| 2026-08-10 | Verified | Focused extension-host coverage passes: 23 media/output policy tests. |
| 2026-08-10 | Implemented | Manifest v11 adds a folder-conflict transaction separate from pending operations. Its first internal-only branch accepts a verified remote folder deletion without mutating Overleaf: it atomically preserves the full local tree as a Keep Both artifact or private Keep Overleaf guard, verifies the remote deletion again, and resumes an interrupted canonical-phase persist after restart. Focused deletion, preservation, pending-child deferral, and restart tests pass (6 passing), with compile and lint clean. |
| 2026-08-10 | Implemented | The internal-only v11 folder transaction now also accepts a verified remote folder replacement without mutating Overleaf: it stages the authoritative remote tree under ignored metadata, rechecks its recursive digest plus folder/parent IDs, atomically preserves the local tree, and installs the stage only while a subtree watcher fence is clear. Focused tests cover Keep Both/Keep Overleaf, canonical-phase restart, same-digest ID replacement, dirty child, ignored local/remote content, pending directory-move, agent local advance, prompt local recreation, watcher deferral, and remote advance after local install. The folder choices remain UI-hidden while exhaustive persisted-phase recovery is incomplete. |
| 2026-08-10 | Corrected | Remote folder replacement now recognizes the exact artifact/guard plus missing canonical root left by a crash after local preservation but before `local-preserved` persisted, promotes only that proven state, and resumes the staged install. Persisted non-blocked folder decisions defer transient proof failures rather than failing the initial pull and replay once per reconnect without duplicate work. Focused restart (Keep Both and Keep Overleaf), transient-defer, and duplicate-connect recovery tests pass. |
| 2026-08-10 | Verified | `npm run compile`, `npm run lint`, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass: 512 extension-host tests. Folder-resolution UI remains hidden; partial-stage/tamper crash recovery and the Hosted tree-mutation CAS boundary remain unchecked. |
| 2026-08-10 | Corrected | A deferred remote-folder replacement now revalidates its protected stage before moving the canonical local tree. A missing stage is recreated only from the still-proven remote root; a partial or tampered stage blocks while leaving its evidence and the canonical local inode/tree untouched. Focused regressions cover both branches. |
| 2026-08-10 | Verified | `npm run compile`, `npm run lint`, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass: 514 extension-host tests. Folder-resolution UI remains hidden; full persisted-phase recovery and the Hosted tree-mutation CAS boundary remain unchecked. |
| 2026-08-10 | Corrected | Persisted non-blocked folder decisions now run before the ordinary initial overwrite. This closes the prepared-to-remote-staged phase-persist interruption: startup resumes the guarded decision rather than classifying its still-local canonical folder as an unrelated create/update. The injected crash/restart regression retains the local artifact and installs only the verified remote tree. |
| 2026-08-10 | Verified | `npm run compile`, `npm run lint`, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass: 515 extension-host tests. Folder-resolution UI remains hidden; full persisted-phase recovery and the Hosted tree-mutation CAS boundary remain unchecked. |
| 2026-08-10 | Corrected | Folder-move finalization now repeats its conflict and file/folder-resolution intersection checks immediately before rekeying manifest and runtime state. A late conflict after remote move acceptance leaves the move journal, original-path conflict proof, and restart state intact for guarded replay rather than silently stranding the conflict. |
| 2026-08-10 | Verified | `npm run compile`, `npm run lint`, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass: 516 extension-host tests. Folder-resolution UI remains hidden; full persisted-phase recovery and the Hosted tree-mutation CAS boundary remain unchecked. |
| 2026-08-10 | Verified | A remote replacement whose local tree was already preserved, then whose protected stage was tampered with before restart, retains the complete local artifact and the tampered stage as evidence. Startup marks the transaction blocked without mutating Overleaf or silently restoring/overwriting either tree. |
| 2026-08-10 | Verified | `npm run compile`, `npm run lint`, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass: 517 extension-host tests. Folder-resolution UI remains hidden; full persisted-phase recovery and the Hosted tree-mutation CAS boundary remain unchecked. |
| 2026-08-10 | Verified | A partial protected remote stage left while the durable record remains prepared is retained as evidence after restart; the canonical local tree (including inode) and Overleaf tree stay unchanged, and the transaction becomes blocked. `npm run compile`, `npm run lint`, and the full extension-host suite pass: 518 tests. Folder-resolution UI remains hidden; full persisted-phase recovery and the Hosted tree-mutation CAS boundary remain unchecked. |

| 2026-08-10 | Implemented | Inbound Overleaf file updates/deletes now stop before mutating a dirty editor's backing file or an ancestor tree, preserve the remote proof as a durable conflict, and still suppress known watcher echoes. Keep Local and every common guarded local write also refuse a dirty exact document, so an agent/Overleaf 3-way merge cannot replace the disk beneath an unsaved buffer. Focused extension-host regressions cover direct update/delete, queued watcher delete, recursive folder delete, and agent-disk plus remote merge. |
| 2026-08-10 | Verified | `npm run compile`, `npm run lint`, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass: 523 extension-host tests. Folder-resolution UI remains hidden; full persisted-phase recovery and the Hosted tree-mutation CAS boundary remain unchecked. |

| 2026-08-10 | Implemented | Manifest v12 adds a guarded regular-file create intent: it persists the exact missing target, parent-folder ID, and stable local digest before the POST, then persists the server-issued document/file ID before its first postcondition lookup. Same-name remote files, including same bytes, are conflicts unless the journal already proves that exact entity under that parent. Missing local parents are created first through the guarded mkdir journal; parent and child watcher queues single-flight the same mkdir proof. |
| 2026-08-10 | Verified | `npm run compile`, `npm run lint`, focused create/parent-watcher regressions, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass: 529 extension-host tests. The next P1 is binding ordinary file delete and binary replacement staging to exact entity/parent IDs; the hosted tree CAS boundary remains unchecked. |

## Context recovery

| 2026-08-10 | Implemented | Recursive local and remote folder deletes now stop before any destructive stage if a durable file update/delete or either side of a pending move lies within the subtree. The child intent remains journaled and visible as pending; neither side is overwritten. Focused extension-host regressions cover a pending child update and a move entering the deleted folder. |
| 2026-08-10 | Verified | Focused folder-delete gate coverage passes (4 extension-host tests, with compile and lint). |
| 2026-08-10 | Implemented | Cross-folder basename changes now call official rename then move on the same entity ID. A request interrupted after either half reconnects to the authoritative tree; the Local Replica move journal recognizes both the new rename-first intermediate and the legacy move-first intermediate, then resumes only when its digest and entity ID still match. Focused VFS and durable-replay regressions pass (2 extension-host tests, with compile and lint). |
| 2026-08-10 | Verified | Full extension-host suite passes: 458 tests, including compile and lint. |
| 2026-08-10 | Implemented | Every text save now refreshes the subscribed authoritative document revision immediately before constructing its OT update, while preserving the pre-refresh local cache as the 3-way merge base. The outgoing update uses that returned current version and still waits for its matching applied revision. Focused pre-submit race regression passes with compile and lint. |
| 2026-08-10 | Verified | Full extension-host suite passes: 459 tests, including compile and lint. |
| 2026-08-10 | Implemented | Manifest v6 adds a durable guarded mkdir intent: it records an absent target and exact parent-folder ID before the request, persists the returned folder ID before postcondition lookup, and accepts only that ID under that parent. A same-name folder after an unacknowledged response remains a conflict. |
| 2026-08-10 | Verified | Focused extension-host mkdir coverage passes: normal journaled create, reconnect replay without another watcher event, restart replay after acknowledged response, and unacknowledged same-name conflict (4 passing; compile and lint clean). |
| 2026-08-10 | Verified | Full extension-host suite passes: 463 tests, including guarded local folder-create journal/reconnect/restart/conflict coverage. |

| 2026-08-10 | Implemented | Manifest v7 adds a durable guarded `rmdir`: local absence, the accepted remote tree digest, exact folder and parent IDs, and the external staging record are bound before remote I/O. Reconnect and restart replay recheck them; a same-tree replacement, a recreated local folder, or an ambiguous response preserves data and becomes a conflict. The VFS rechecks the expected parent at the mutation boundary and verifies exact-ID absence before manifest cleanup. Official Overleaf tree HTTP endpoints are ID-scoped and expose no conditional parent/revision/ETag precondition, so the remaining in-flight server race has the same last-operation semantics as the web client; Local Replica does not claim a stronger server-side CAS guarantee. |
| 2026-08-10 | Verified | `npm run compile`, `npm run lint`, 14 focused rmdir/legacy-journal regressions, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass (470 extension-host tests). |

| 2026-08-10 | Implemented | Manifest v8 records a selected local directory's stable `dev:ino` alongside the accepted remote folder and parent IDs. Legacy manifests migrate safely; a missing/non-file-system identity remains non-move evidence rather than guessed ownership. This is the durable local proof required before correlating a folder rename/move. |
| 2026-08-10 | Verified | `npm run compile` and focused manifest/mkdir/reconnect coverage pass (4 extension-host tests). |

| 2026-08-10 | Implemented | Manifest v9 journals an ID-preserving folder move before remote I/O with the source/destination paths, exact folder and parent IDs, full recursive digest, and local directory `dev:ino`. It resumes a cross-parent rename/move from an authoritative intermediate path, rekeys descendant manifest/cache state only after acceptance, and preserves conflicts for an occupied destination, same-tree ID replacement, or unresolved child intent. A child changed by an agent while the folder move is in flight is forced through a later guarded re-scan. |
| 2026-08-10 | Corrected | Directory reconciliation now treats a child stable-snapshot block as a recoverable keyed retry instead of stranding a parent operation. Mapping removal flushes an accepted held file/folder delete rather than clearing it during the rename-candidate window. |
| 2026-08-10 | Corrected | A local child watcher event observed after folder-move verification but before manifest rekey is now buffered, then re-enqueued only after the accepted folder identity and descendant paths are final. The destination child is therefore never silently dropped during that finalization window. |
| 2026-08-10 | Verified | Focused folder-move coverage passes (10 tests, including the post-verification child-event race), and the full xvfb-run VSCODE_TEST_VERSION=1.130.0 npm test command passes: 479 extension-host tests, including compile and lint. The hosted-API CAS release boundary remains explicitly unchecked. |

| 2026-08-10 | Audited | Revalidated the HTTP bootstrap-to-socket handoff against the official join ordering. Bootstrapped text remains pending until a current joinDoc read reaches the normal guarded pull path; compilation waits for that set. Four deterministic handoff cases pass: remote advance, concurrent local/remote changes, failed join then recovery, and teardown. |

| 2026-08-10 | Implemented | Manifest v10 adds a separate durable file/binary conflict-resolution transaction and bounded history. The Local Replica settings flow now offers Keep Local, Keep Overleaf, and (when a local file exists) Keep Both. Keep Overleaf and Keep Both never mutate Overleaf: they verify the recorded remote revision, atomically install its local canonical state, and Keep Both retains the original local bytes under ignored extension metadata. Recovery proves the remote state and artifact before finalizing after restart. An intersecting pending folder move or active folder-move transaction fail-closes before any canonical replacement or path rekey. Focused conflict coverage covers binary replacement/deletion, artifact preservation, same-byte inode replacement, remote advancement, prepared/local-preserved/canonical-applied restart boundaries, picker cancellation, and folder-move isolation (14 passing; compile and lint clean). |
| 2026-08-10 | Verified | `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 493 extension-host tests, including compile and lint. |

| 2026-08-10 | Planned | The next folder-resolution slice is explicitly separated from file/binary v10: it requires a v11 guarded tree transaction with recursive digest, folder/parent identity, local directory identity, protected artifact/staging, and restart phases. It will remain UI-hidden until local/remote tree replacement and folder deletion have deterministic no-loss coverage. |

| 2026-08-11 | Implemented | Manifest v13 binds ordinary regular-file update/delete journals to the exact target entity and parent IDs. Text writes recheck the expected identity before and after their OT path; media replacement and delete stage/restore/remove calls use guarded VFS ID operations. A same-byte collaborator replacement or identity-less legacy journal becomes a durable conflict rather than an acknowledgement. |
| 2026-08-11 | Corrected | The equal-content push fast path now persists the current entity proof before accepting its readback, so an agent advancing the local file during that snapshot re-drives a guarded next push instead of losing its accepted identity. Legacy staged binary recovery preserves/rolls back evidence without treating path/bytes as proof; v2 recovery cleans only the exact original stage. |
| 2026-08-11 | Verified | `npm run compile`, `npm run lint`, focused create/reconnect/restart, guarded delete/replacement, and stable-snapshot coverage pass; `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` passes: 535 extension-host tests. |

| 2026-08-11 | Verified | The internal-only folder-resolution transaction now has deterministic crash/restart coverage for deletion and replacement across prepared, remote-staged, local-preserved, and canonical-applied boundaries. It verifies full artifact/guard trees, same-digest remote entity replacement, local advancement, watcher/move fences, missing/partial/tampered stages, and no silent remote mutation. The folder UI and hosted tree-CAS release boundary remain intentionally unchecked. |
| 2026-08-11 | Verified | `npm run compile`, `npm run lint`, focused deletion-phase recovery, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass: 536 extension-host tests. |

| 2026-08-11 | Implemented | Manifest v14 adds an opt-in Base/Local/Overleaf text-conflict review with an extension-owned CSP-locked three-pane preview, editable in-memory result, and native confirmation. The merge proof retains bounded UTF-8 base bytes plus exact remote document/parent identity. Applying a result is a separate durable transaction that rechecks local inode/digest, active journals and folder moves, and remote revision/entity immediately before each local or guarded OT mutation. |
| 2026-08-11 | Verified | Text-merge regressions cover overlapping conflict markers, preview cancel, actual webview-message routing, dirty editors before/after review, local inode and same-byte remote-entity replacement, directory-move fences, agent writes after canonical install, and interrupted phase recovery. Compile, focused coverage (17 passing), and the full extension-host suite pass (546 tests). |

| 2026-08-11 | Implemented | The registered Local Replica conflict picker now exposes folder Keep Overleaf and Keep Both only for directory/missing pairs. It never offers folder Keep Local and therefore never uses an unconditioned Hosted tree mutation; file-folder type swaps remain an explicit fail-closed review state. Both visible choices call the already durable remote-authoritative folder transaction, preserve its stage/artifact/guard evidence, and reveal a preserved tree in Explorer. |
| 2026-08-11 | Verified | Picker regressions cover remote-only choices, Keep Both tree artifact, Keep Overleaf private guard, confirmation cancellation, missing-local limitation, both-missing file classification, and file-folder type rejection. Compile, focused coverage (5 passing), and the full extension-host suite pass (551 tests). The Hosted tree-CAS release boundary for folder Keep Local remains unchecked. |

After compaction, read this document first. Then inspect `git status -sb`, the
latest work-log row, and the next unchecked item in the active phase. Do not
skip to packaging or release while any P0/P1 acceptance item remains unchecked.

| 2026-08-11 | Implemented | Manifest v15 binds regular-file move intent to the original source and destination folder IDs as well as the file ID and local inode/digest. Before each rename/move it re-resolves both parents, passes the recorded source parent into the VFS identity guard, and verifies the moved entity at its recorded destination parent. A v2 legacy move upgrades only when the trusted manifest proves both historical parents; otherwise it preserves both sides as a durable conflict. Folder moves now have the same immediate two-parent proof. The remaining Hosted API check-to-request CAS boundary remains explicitly open. |

| 2026-08-11 | Verified | `npm run compile`, `npm run lint`, focused move-parent identity coverage (6 passing), and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` all pass: 557 extension-host tests. |

| 2026-08-11 | Corrected | Live demo validation exposed a watcher race in which a delayed PNG/ZIP destination proof could outlast the 500ms source-delete candidate window. The source delete then raced the valid ID-preserving move and left a false conflict/journal. A verified destination now claims its exact source inode/digest before awaiting remote proof; if proof later fails it remains a conflict, never a second delete. A deterministic delayed-binary regression passes. |

| 2026-08-11 | Implemented | Local folder moves now tolerate both watcher and socket event reordering: child-first local events claim the enclosing inode/tree move, new recursive parents gate child writes until their guarded mkdir is accepted, and delayed VFS rename/move self-echoes are idempotent. Remote source/destination/rename-first events are fenced behind the exact folder-move journal, rechecked after debounce and inside the queued pull, tagged by journal ID, and reclassified when that journal conflicts. A first file create advanced by an agent is promoted to a guarded update/delete using the exact newly created entity instead of becoming a false same-name-create conflict. |

| 2026-08-11 | Verified | `npm run compile`, `npm run lint`, focused watcher/socket/agent-create coverage, and `xvfb-run -a env VSCODE_TEST_VERSION=1.130.0 npm test` pass: 562 extension-host tests. The Hosted tree-CAS release boundary remains unchecked. |
